# ── Core LLM & Orchestration (LangChain / LangGraph 1.x) ───────────────────────
# 1.0 floors are required: agents use langchain.agents.create_agent (the v1
# replacement for the deprecated langgraph.prebuilt.create_react_agent).
langchain>=1.0
langchain-core>=1.0
langgraph>=1.0
langchain-community>=0.3.0          # document loaders only (sunset; loaders still live here)
langchain-ollama>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
langchain-anthropic>=0.3.0
langchain-groq>=0.2.0
langchain-openai>=0.2.0             # openai + any OpenAI-compatible endpoint (vLLM, LM Studio, OpenRouter)
langchain-huggingface>=0.1.0        # local embeddings (replaces deprecated community HuggingFaceEmbeddings)
langchain-google-genai>=2.0.0       # optional: reachable via LLM_PROVIDER=google_genai
# Any other LangChain provider works the same way — install its langchain-<provider>
# extra (e.g. langchain-mistralai, langchain-aws) and set LLM_PROVIDER accordingly.

# ── RAG / Memory ──────────────────────────────────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0          # local embeddings, no API key needed
langchain-chroma>=0.1.0
pypdf>=4.0.0                          # PDF ingestion
unstructured[md]>=0.15.0              # markdown / text ingestion

# ── Episodic memory ───────────────────────────────────────────────────────────
mem0ai>=0.1.0                         # Mem0 local mode (no API key required) — default backend
# langmem>=0.0.30                     # opt-in backend (EPISODIC_BACKEND=langmem): LangGraph-native
#                                     #   consolidation; verified compatible with the 1.x stack.

# ── Tools ─────────────────────────────────────────────────────────────────────
ddgs>=9.0.0                   # free web search

# ── MCP ───────────────────────────────────────────────────────────────────────
langchain-mcp-adapters>=0.1.0
mcp>=1.0.0

# ── UI ────────────────────────────────────────────────────────────────────────
gradio>=4.44.0
plotly>=5.0.0                         # dashboard charts (Tab 1 & 2)

# ── Observability ─────────────────────────────────────────────────────────────
langfuse>=2.0.0                       # optional: set LANGFUSE_* env vars to activate
# langsmith is activated via LANGCHAIN_API_KEY env var, no extra install needed

# ── Evals ─────────────────────────────────────────────────────────────────────
ragas>=0.1.0                          # RAG evaluation metrics
datasets>=2.0.0                       # HuggingFace datasets (required by ragas)
agentevals>=0.0.9                     # agent trajectory + tool-call-correctness evals

# ── Office document extraction (Auditor domain layer) ─────────────────────────
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=0.6.0

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.0            # PostgreSQL driver (chase/db.py)

# ── Utilities ─────────────────────────────────────────────────────────────────
tenacity>=8.0.0                  # bounded retries around LLM calls (agents/resilience.py)
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
loguru>=0.7.0
python-telegram-bot>=20.0
